# V0.2.1 Appearance Picker Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Match the approved Apple-style Appearance layout using the supplied System, Light, and Dark preview images.

**Architecture:** Add the three PNG files as SwiftPM resources for `SkillDockApp`. Replace the generated preview artwork with resource-backed images and move the Appearance title into the same gray rounded container as the right-aligned choices.

**Tech Stack:** Swift 6.3, SwiftUI, SwiftPM resources, macOS 26

---

### Task 1: Add Appearance Preview Resources

**Files:**
- Modify: `Package.swift`
- Create: `Sources/SkillDockApp/Resources/Appearance/System.png`
- Create: `Sources/SkillDockApp/Resources/Appearance/Light.png`
- Create: `Sources/SkillDockApp/Resources/Appearance/Dark.png`

- [x] Copy the approved preview images into the app target resources.
- [x] Declare `SkillDockApp` resources in `Package.swift`.
- [x] Build to prove the resources are bundled.

### Task 2: Match The Approved Layout

**Files:**
- Modify: `Sources/SkillDockApp/Views/AppearanceModePicker.swift`
- Modify: `Sources/SkillDockApp/Views/SettingsView.swift`

- [x] Remove the Form section title for Appearance.
- [x] Put Appearance title and all three choices inside one gray rounded container.
- [x] Keep the title left aligned and choices right aligned.
- [x] Place each mode name below its image.
- [x] Show the selected mode with a blue rounded outline and bold name.

### Task 3: Verify And Record

**Files:**
- Modify: `docs/CURRENT_STATE.md`

- [x] Run `swift test`.
- [x] Run `swift build -c release`.
- [x] Package and launch the app for visual inspection.
- [x] Record the Appearance picker polish in current state.
